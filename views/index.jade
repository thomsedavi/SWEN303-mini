extends ../node_modules/jade-bootstrap/_bootstrap
head
  title=title
block body
  +navbar("SWEN303", "dropdown_menu")
    +nav_item("/", "active") Home
    +nav_item("/users") Users
  .container
    h1 #{title}
    p Welcome to #{title}. A collection of documents!
    +input("text","txtName","Type something here","Search box")
    +checkbox("Check Box")
    +radio("Radio Button")
    +submit("Submit")